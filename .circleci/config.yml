version: 2.1

orbs:
  win: circleci/windows@2.2.0

jobs:
  build:
    executor:
      name: win/default
    steps:
      - run:
         command: uname
         shell: bash.exe
      - run:
         command: python --version
         shell: bash.exe

