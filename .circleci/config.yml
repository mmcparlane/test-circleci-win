version: 2.1

orbs:
  win: circleci/windows@2.2.0

jobs:
  build:
    executor:
      name: win/default
    steps:
      - run:
         command: ls /usr/bin
         shell: bash.exe
      - run:
         command: ls /etc
         shell: bash.exe
